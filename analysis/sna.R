library(igraph)

# Visualize whole network
# networkWhole <- graph_from_data_frame(networks)
# plot(networkWhole, layout = layout.fruchterman.reingold, vertex.size = 5,
#      edge.width = 2, vertex.label = NA)
# get.vertex.attribute(networkWhole)
